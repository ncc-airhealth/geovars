# geovars

`geovars` is a Python library for calculating geospatial variables with DuckDB and DuckDB Spatial.

The library provides a lightweight interface for running reusable SQL-based geospatial calculations against point datasets and geospatial reference data. It is designed to keep geospatial variable logic organized, reproducible, and easy to integrate into data pipelines.
